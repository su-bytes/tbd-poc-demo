apiVersion: v1
kind: Service
metadata:
  name: order-svc-lb
spec:
  type: LoadBalancer  # or ClusterIP with Ingress
  selector:
    app: order-svc
    version: {{ .Values.activeVersion }}  # "blue" or "green"
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
